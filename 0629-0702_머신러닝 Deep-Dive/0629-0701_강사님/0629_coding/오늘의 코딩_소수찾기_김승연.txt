from itertools import permutations
def solution(numbers):
    answer = 0
    lst_num = list(numbers)
    permute = []
    per = []
    for i in range(1, len(lst_num)+1):
        permute += list(map(''.join, permutations(lst_num, i)))
    for j in permute:
        per.append(int(j))
    per = list(set(per))
    print(per)
    for k in per:
        a = False
        if k not in [0,1,2]:
            for l in range(2, k//2+1):
                if k % l == 0:
                    a = True
                    break
            if a == False:
                    answer += 1
        elif k == 2:
            answer += 1
            pass
        else:
            pass
    return answer